// Stories from people you follow
// → circular avatars in a row
// → click → opens that person's story fullscreen
// → completely separate from posts
import React from "react";

function FeedSlider() {
  return <div>Stories here ... </div>;
}

export default FeedSlider;
